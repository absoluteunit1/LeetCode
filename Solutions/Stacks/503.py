# My first solution 
# Passes 224/224 test cases but takes too long. 
def solution(nums):
    res, stack = [], []
    if len(nums)>1:
        for i in range(len(nums)):
            
    elif len(nums) == 1:
        return [-1]
    elif len(nums) == 0:
        return []
    return res